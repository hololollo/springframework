<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
"-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<select id="getBoardList" resultType="com.spring3.dto.Board">
		select * from board order by resdate desc
	</select>
		<select id="getBoard" resultType="com.spring3.dto.Board">
		select * from board where id = #{id}
	</select>
	<select id ="maxNum" resultType="int">
		select bno from board order by bno desc limit 1;
	</select>
	<insert id="insBoard" parameterType="com.spring3.dto.Board"> <!-- parametrtType은 생략도 가능. -->
		insert into board values (#{bno}, #{title}, #{content}, #{author}, 
		default, default)
	</insert>
		<update id="upBoard">
		update board set title=#{title}, content=#{content} where bno=#{bno}
	</update>
		<update id="delBoard">
		delete from board where bno=#{bno}
	</update>
</mapper>
<!-- namespace나 해당 명령의 id는 중복되면 절대 안되며 메서드 이름을 의미합니다. -->